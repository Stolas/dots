- clean: ['~']

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    # Todo; git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

- link:
    ~/.Xresources: Xresources
    ~/.tmux.conf: tmux.conf
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.taskrc: taskrc
    ~/.vimrc: vimrc
    ~/.taskrc: taskrc
    ~/.zshrc: zshrc
    ~/.crawlrc: crawlrc

    ~/scripts: scripts
