- clean: ['~']

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.Rprofile: Rprofile
    ~/.Xresources: Xresources
    ~/.crawlrc: crawlrc
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.inputrc: inputrc
    ~/.taskrc: taskrc
    ~/.taskrc: taskrc
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc

    ~/scripts: scripts
